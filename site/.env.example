# Copy to .env and fill in. Both values are public (the anon key is read-only
# and RLS-protected). Never use the service_role key in this app.
VITE_SUPABASE_URL=https://YOUR_PROJECT.supabase.co
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key
